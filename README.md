This repository now contains a static portfolio site for Drona.

Files of interest in the root:
- index.html
- styles.css
- app.js

To preview locally, open index.html in a browser or run a local static server (e.g. python -m http.server).

GitHub Pages workflow publishes repo root to gh-pages branch.
